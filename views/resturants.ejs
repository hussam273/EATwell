<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("includes/head") %>
    <link rel="stylesheet" href="styles/resturant.css">
  </head>
  <body>
    <%- include("includes/nav") %>
    <main>
      <div class="main-info">
        <h1>Recommended restaurants</h1>
        <% if(numberOfResturants === 0) { %>
          <p>unfotunately we dont hae any resturants yet - maybe start recomending some</p>
        <% } else { %>
          <p>Find your next favorite restaurants with help of our other users!</p>
          <p>we found <%= numberOfResturants %> resturants.</p>
        <% } %>
      </div>
      <ul class="resturants">
        <% for (const resturant of resturants) { %>
          <li class="resturant">
            <h2><%= resturant.name %></h2>
            <p class="adress"><%= resturant.type %></p>
            <p class="adress"><%= resturant.adress %></p>
            <p class="description"><%= resturant.reasons %></p>
            <div>
              <a class="btn btn-1" href="<%= resturant.website %>">View Website</a>
            </div>
          </li>
        <% } %> 
      </ul>
    </main>
  </body>
</html>